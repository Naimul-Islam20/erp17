/** Primary site CTA — matches header “Get Started” (sky, rounded-full). */
export const primaryCtaClassName =
  "inline-block px-4 py-3 bg-[var(--primary)] text-white font-bold rounded-full hover:bg-[var(--primary-hover)] transition text-center";

/** Same style with inline-flex for icon + label (e.g. clients marquee). */
export const primaryCtaInlineFlexClassName =
  "inline-flex items-center justify-center gap-2 px-4 py-3 bg-[var(--primary)] text-white font-bold rounded-full hover:bg-[var(--primary-hover)] transition whitespace-nowrap text-sm sm:text-base";

/** Secondary outline — paired with primary on clients section. */
export const secondaryOutlineCtaClassName =
  "inline-flex items-center justify-center gap-2 px-4 py-3 border border-[var(--primary)] text-[var(--primary)] font-bold rounded-full hover:bg-sky-50 transition whitespace-nowrap text-sm sm:text-base";
